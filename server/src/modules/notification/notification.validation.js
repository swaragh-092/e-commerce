'use strict';

const Joi = require('joi');

// No endpoints for notification in phase 1 (it is an internal service)
// Admin CRUD typically added in Phase 5 or when explicitly requested
module.exports = {};
